import React from 'react'
import expect from 'expect'

import {ArticleItem} from 'ArticleItem.jsx'

describe('ArticleItem component', () => {
    it('should exist', () => {
        expect(ArticleItem).toExist()
    });
});