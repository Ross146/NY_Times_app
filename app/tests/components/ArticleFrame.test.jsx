import React from 'react';
import expect from 'expect';

import {ArticleFrame} from 'ArticleFrame.jsx'

describe('ArticleFrame component', () => {
    it ('should exist', () => {
        expect(ArticleFrame).toExist()
    });
});